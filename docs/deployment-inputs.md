# 部署前待提供信息

状态：待用户/学校补充外部值
更新日期：2026-08-26

本清单只记录标识、责任和准备状态。不得把密码、AccessKey Secret、Cookie 密钥、CDN 鉴权 key、私钥或真实相册口令写入本文件或 Git。

## 1. 门禁分类

- **编码前必需**：缺失会改变代码契约，开始编码前必须确定。
- **号码识别阶段前必需**：不阻塞基础照片功能，但没有它不能校准或验收自动 OCR。
- **部署前必需**：代码通过环境变量适配，部署前提供即可。
- **首场活动前必需**：不阻塞本地开发，但没有它不得使用真实学生影像。

当前架构决定已经完整；以下待填项目都是外部值或组织责任，不是留给实现者的技术选择。

## 2. 产品与学校信息

| 项目 | 门禁 | 当前值/状态 | 责任人 |
| --- | --- | --- | --- |
| 产品主名称 | 编码前 | 已确定：中学部影像直播 | 用户 |
| 浏览器完整标题 | 编码前 | 默认：北航实验学校中学部｜影像直播；待确认正式校名写法 | 学校 |
| Logo 文件 | 部署前 | 待提供；建议 SVG 或透明 PNG，并确认使用授权 | 学校 |
| 默认强调色 | 部署前 | 待提供；作为 `primary` 候选逐状态校验 WCAG AA，失败或未提供时使用可访问的中性蓝色 | 学校 |
| 隐私说明正文 | 首场活动前 | 待学校审核 | 学校 |
| 未成年人影像授权流程 | 首场活动前 | 待确认 | 学校 |
| 删除/投诉联系人 | 首场活动前 | 待提供姓名/岗位和公开联系方式 | 学校 |
| 安全事件联系人 | 部署前 | 待提供，不一定公开展示 | 学校/运维 |
| 首场试运行活动 | 首场活动前 | 待确定日期、预计人数和照片数量 | 用户 |
| 号码找照片隐私说明 | 首场活动前 | 待学校确认号码用途、访问范围和删除流程 | 学校 |
| 号码牌评测照片 | 号码识别阶段前 | 已有 `test_photos/` 813 张 Git 忽略本地 JPG；仍待学校确认评测授权并提供逐号码/四边形真值标注 | 用户/学校 |
| 评测标注责任人 | 号码识别阶段前 | 待指定；需标注人眼可读号码、四边形，并对无号码照片明确标注 | 用户/学校 |
| 号码规则样例 | 号码识别阶段前 | 已确定支持任意位置/区间规则；需提供首场活动实际配置用于评测 | 用户 |
| 年级/班级选项与映射 | 号码识别阶段前 | 待提供正式名称、排序和位置映射，例如第 1 位到年级、第 2–3 位到班级 | 用户/学校 |
| 无号码复核口径 | 号码识别阶段前 | 已确定只能人工确认；学校需指定现场由上传者还是审核员优先处理 | 用户/学校 |

## 3. 主站域名与 TLS

| 项目 | 门禁 | 当前值/状态 |
| --- | --- | --- |
| `APP_ORIGIN` | 部署前 | 待提供自有未备案域名，主站/API 同源 |
| 主站 DNS 控制权 | 部署前 | 待确认用户可自行修改 A/AAAA/CNAME |
| 主站证书方式 | 部署前 | 目标为 Caddy 自动 HTTPS；需确认 80/443 和 DNS 可用 |
| 是否已有 IPv6 | 部署前 | 待确认；没有时不得发布无效 AAAA |
| HSTS 上线时机 | 部署前 | 首次 HTTPS 验证通过后再开启，不在调试期预加载 |

## 4. 香港主机

| 项目 | 门禁 | 当前值/状态 |
| --- | --- | --- |
| 云厂商/地域 | 部署前 | 已知：其他云、香港；具体厂商待提供 |
| CPU/内存 | 已知 | 2 vCPU / 2GB RAM |
| 操作系统与版本 | 部署前 | 待提供；目标为受支持 x86_64/arm64 Linux |
| CPU 架构 | 部署前 | 待提供 |
| 数据盘可用空间 | 部署前 | 待提供；需满足 PostgreSQL、日志和恢复临时空间 |
| Docker/Compose 版本 | 部署前 | 待检查，不在文档阶段安装 |
| SSH 运维方式 | 部署前 | 待提供；不得提交私钥 |
| 防火墙/安全组 | 部署前 | 只开放 22（受限）、80、443；数据库不公网开放 |
| Swap | 部署前 | 待检查；2GB 主机建议配置受控 swap 作为 OOM 兜底 |
| 时间同步/时区 | 部署前 | NTP 正常；系统 UTC，业务展示 Asia/Shanghai |
| 主机快照/备份 | 部署前 | 待确认云厂商基础能力；数据库另有 OSS 加密逻辑备份 |
| 出站访问 | 部署前 | 需能访问杭州 OSS、阿里云控制 API、证书服务和容器镜像源 |

## 5. 阿里云 OSS

| 项目 | 门禁 | 当前值/状态 |
| --- | --- | --- |
| 阿里云账号 UID | 部署前 | 待提供标识，不提供主账号密码 |
| 媒体 Bucket 名 | 部署前 | 待创建/提供；必须杭州、标准、私有 |
| 备份 Bucket 名 | 部署前 | 待创建/提供；必须杭州、标准、私有且不绑定 CDN |
| Bucket 所属账号 | 部署前 | 应与 CDN 同账号；待确认 |
| CORS 主站 Origin | 部署前 | 由最终 `APP_ORIGIN` 派生 |
| OCR 模型静态前缀 | 部署前 | 固定为 `/assets/models/bib-ocr/{modelVersion}/`，需验证长缓存和 CORS |
| OCR 模型/运行时哈希 | 部署前 | 编码阶段锁定并记录；不能运行时使用第三方 CDN |
| CDN 私有回源角色 | 部署前 | 待确认已授权且只读媒体 Bucket |
| 应用 RAM AccessKey ID | 部署前 | 只通过秘密存储提供，不写文档 |
| 应用 RAM Policy | 部署前 | 按 `08-aliyun-cdn-oss.md` 最小权限创建并审查 |
| OSS 资源包/计费方式 | 部署前 | 待用户在控制台确认，不由代码购买 |

## 6. CDN 与 `cdn.cloverta.top`

| 项目 | 门禁 | 当前值/状态 |
| --- | --- | --- |
| CDN 控制权 | 已知 | 用户在自己的阿里云账号全权控制 |
| DNS 控制权 | 已知 | 由朋友控制，需要对方执行记录变更 |
| 备案状态 | 部署前 | 用户称已备案；需在控制台重新验证 |
| 当前 CNAME | 部署前 | 待查询并记录 |
| 当前业务类型 | 部署前 | 待读取；目标为图片小文件 |
| 当前源站 | 部署前 | 待读取；切换前保存原配置 |
| HTTPS 证书与到期日 | 部署前 | 待查询 |
| 证书续期责任 | 部署前 | 用户与域名所有者需明确 |
| URL 鉴权主/备 key | 部署前 | 通过秘密存储提供，不写文档 |
| CDN 资源包/计费方式 | 部署前 | 待用户确认，不由代码购买 |
| 费用提醒接收人 | 部署前 | 待提供 |
| 域名使用授权/迁移预案 | 首场活动前 | 建议与朋友明确长期授权和替代域名流程 |

## 7. 内部账号

| 项目 | 门禁 | 当前值/状态 |
| --- | --- | --- |
| 首位管理员用户名/展示名 | 部署前 | 待提供；密码通过一次性安全流程设置 |
| 初始上传者数量 | 首场活动前 | 目标不超过 5；待列展示名与角色 |
| 初始审核员数量 | 首场活动前 | 待提供 |
| 账号停用/离校流程 | 首场活动前 | 待学校指定负责人 |

## 8. 部署秘密变量名

后续实现应使用至少以下变量名；本文件永远不填写值：

- `APP_ORIGIN`
- `MEDIA_BASE_URL`
- `PHOTO_UPLOAD_BASE_URL`（精确 OSS 上传 origin，不含对象路径或签名）
- `DATABASE_URL`
- `SESSION_SECRET_CURRENT`
- `SESSION_SECRET_PREVIOUS`
- `CSRF_SECRET`
- `CURSOR_SIGNING_SECRET`
- `ALIYUN_OSS_REGION`
- `ALIYUN_OSS_MEDIA_BUCKET`
- `ALIYUN_OSS_BACKUP_BUCKET`
- `ALIYUN_ACCESS_KEY_ID`
- `ALIYUN_ACCESS_KEY_SECRET`
- `ALIYUN_CDN_AUTH_KEY_CURRENT`
- `ALIYUN_CDN_AUTH_KEY_PREVIOUS`
- `BACKUP_ENCRYPTION_PUBLIC_KEY_FILE`
- `BACKUP_DECRYPTION_PRIVATE_KEY_FILE`（仅隔离恢复时临时提供）
- `BOOTSTRAP_ADMIN_TOKEN`
- `BIB_DATA_KEY`
- `BIB_DATA_KEY_PREVIOUS`
- `BIB_SEARCH_KEY`
- `BIB_SEARCH_KEY_PREVIOUS`
- `BIB_KEY_VERSION`
- `BIB_KEY_VERSION_PREVIOUS`

具体命名可在编码阶段保持语义不变地调整，但必须有启动时 schema 校验、禁止打印和轮换说明。

## 9. 部署批准记录

真正部署前应在变更记录中明确：

- 获准修改哪些 OSS/CDN/DNS 项；
- 谁执行 DNS 和证书步骤；
- 使用哪个测试前缀和测试相册；
- 允许产生的最大小流量联调范围；
- 回滚到哪个 CDN/源站配置；
- 何时检查账单和由谁确认。
